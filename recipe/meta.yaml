{% set name = "pygeohash" %}
{% set version = "2.0.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 9ae38a251db0ab0f56bf55ce376b111bb1c1518795a092c01956ee00890be091

build:
  noarch: python
  number: 0
  script: "{{ PYTHON }} -m pip install . --no-deps -vv"

requirements:
  host:
    - python
    - pip
  run:
    - python

test:
  imports:
    - pygeohash

about:
  home: https://github.com/wdm0006/pygeohash
  license: AGPL-3.0
  license_family: AGPL
  license_file: LICENSE.txt
  summary: 'A module for decoding and encoding geohashes to and from latitude and longitude coordinates'

  description: |
    Pygeohash is a Python module that provides functions for
    decoding and encoding geohashes to and from latitude and
    longitude coordinates, and doing basic calculations and
    approximations with them.


extra:
  recipe-maintainers:
    - mxr-conda
    - rluria14
    - adbeda
    - agcopenhaver
    - oblute
